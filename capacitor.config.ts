import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.muntadher.asd',
  appName: 'muntadher.asd',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
